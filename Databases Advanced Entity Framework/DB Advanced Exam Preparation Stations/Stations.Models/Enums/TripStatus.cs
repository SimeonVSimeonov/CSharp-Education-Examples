using System;
using System.Collections.Generic;
using System.Text;

namespace Stations.Models.Enums
{
    public enum TripStatus
    {
        OnTime = 0,
        Delayed = 1,
        Early = 2

        //"OnTime", "Delayed" and "Early" (default: "OnTime")
    }
}
